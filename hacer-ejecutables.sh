#!/bin/bash
chmod +x separate-project.sh
chmod +x start-dev.sh
chmod +x test-local.sh
chmod +x verificar.sh
chmod +x instalar.sh
chmod +x preparar-easypanel.sh
echo "✅ Permisos actualizados para todos los scripts"
